using UnityEngine;

public class LevelScene : MonoBehaviour
{
    Fader fader;
    private void Start()
    {
        fader = GetComponent<Fader>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
